import React, { useState } from "react";
import "./style.css";

function SearchForm({ searchFor }) {

  return (
    <div>
      <form>
        <input />
        <button type="submit">
          Submit
        </button>
      </form>
    </div>
  );
}

export default SearchForm;